// Kept as a re-export so any existing imports still resolve. The real
// company config lives in `app/lib/company.ts` and is shared with
// invoice / jobwork-bill print templates.
export { COMPANY } from '@/lib/company';
