/**
 * Re-export — the component now lives in app/components/reminders so it
 * can be shared with the dashboard widget. Kept here so any existing
 * relative imports (`./delete-reminder-button`) keep working.
 */
export { DeleteReminderButton } from '@/app/components/reminders/delete-reminder-button';
